import React from 'react';
import {View, Text} from 'react-native';

const Card = ({movie, index}) => {
    return (
        <View>
            <Text>
                Card
            </Text>
        </View>
    );
};

export default Card;